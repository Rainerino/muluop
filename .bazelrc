# Common settings
common --enable_bzlmod
common --symlink_prefix=build/
common --distdir=third_party/dists
# Test output
test --test_output=errors

build --disk_cache=~/.bazel_cache
build --experimental_disk_cache_gc_idle_delay=5m
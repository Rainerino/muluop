load("//tools/jupyter:defs.bzl", "jupyter_lab")

# This target will have pandas, numpy, jupyter, and everything else automatically.
jupyter_lab(
    name = "lab",
    # You can add local libraries (cc_library, py_library) here if needed
    deps = [
        
    ],
)

package(default_visibility = ["//visibility:public"])

# You must export these so the macro can use them in other packages
exports_files([
    "defs.bzl",
    "wrapper.py",
    "kernel_wrapper.py",  # <--- ADD THIS
])
import sys
import os

# Re-execute with ipykernel
# We use the "-m" flag to ensure we use the installed module correctly
if __name__ == "__main__":
    from ipykernel import kernelapp as app
    app.launch_new_instance()